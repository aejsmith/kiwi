diff -urN gcc-4.5.1.orig/gcc/Makefile.in gcc-4.5.1/gcc/Makefile.in
--- gcc-4.5.1.orig/gcc/Makefile.in	2010-08-13 11:25:22.953918149 +0100
+++ gcc-4.5.1/gcc/Makefile.in	2010-08-13 11:25:30.368670220 +0100
@@ -3953,7 +3953,7 @@
 	      gcc_dir=`${PWD_COMMAND}` ; \
 	      export TARGET_MACHINE srcdir SHELL MACRO_LIST && \
 	      cd $(build_objdir)/fixincludes && \
-	      $(SHELL) ./fixinc.sh "$${gcc_dir}/$${fix_dir}" \
+	      $(SHELL) -c true "$${gcc_dir}/$${fix_dir}" \
 	        $(SYSTEM_HEADER_DIR) $(OTHER_FIXINCLUDES_DIRS) ); \
 	    rm -f $${fix_dir}/syslimits.h; \
 	    if [ -f $${fix_dir}/limits.h ]; then \
