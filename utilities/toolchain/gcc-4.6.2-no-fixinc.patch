diff -urN gcc-4.6.2.orig/gcc/Makefile.in gcc-4.6.2/gcc/Makefile.in
--- gcc-4.6.2.orig/gcc/Makefile.in	2011-11-19 10:21:13.000000000 +0000
+++ gcc-4.6.2/gcc/Makefile.in	2011-11-19 10:23:45.000000000 +0000
@@ -4168,7 +4168,7 @@
 	      gcc_dir=`${PWD_COMMAND}` ; \
 	      export TARGET_MACHINE srcdir SHELL MACRO_LIST && \
 	      cd $(build_objdir)/fixincludes && \
-	      $(SHELL) ./fixinc.sh "$${gcc_dir}/$${fix_dir}" \
+	      $(SHELL) -c true "$${gcc_dir}/$${fix_dir}" \
 	        $(SYSTEM_HEADER_DIR) $(OTHER_FIXINCLUDES_DIRS) ); \
 	    rm -f $${fix_dir}/syslimits.h; \
 	    if [ -f $${fix_dir}/limits.h ]; then \
