diff -urN gcc-4.8.1.orig/gcc/Makefile.in gcc-4.8.1/gcc/Makefile.in
--- gcc-4.8.1.orig/gcc/Makefile.in	2013-07-04 11:03:07.000000000 +0100
+++ gcc-4.8.1/gcc/Makefile.in	2013-07-04 11:07:41.000000000 +0100
@@ -4213,7 +4213,7 @@
 	      gcc_dir=`${PWD_COMMAND}` ; \
 	      export TARGET_MACHINE srcdir SHELL MACRO_LIST && \
 	      cd $(build_objdir)/fixincludes && \
-	      $(SHELL) ./fixinc.sh "$${gcc_dir}/$${fix_dir}" \
+	      $(SHELL) -c true "$${gcc_dir}/$${fix_dir}" \
 	        $(SYSTEM_HEADER_DIR) $(OTHER_FIXINCLUDES_DIRS) ); \
 	    rm -f $${fix_dir}/syslimits.h; \
 	    if [ -f $${fix_dir}/limits.h ]; then \
